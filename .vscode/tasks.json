{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Eun Server",
            "type": "shell",
            "command": "uv run app.py",
            "options": {
                "cwd": "${workspaceFolder}/my_portfolio"
            }
        }
    ]
}